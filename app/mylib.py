"""Simple helper functions used by the demo compute window."""

import math


def compute(value):
    """
    Return a deterministic demo computation for the GUI example.
    """
    return math.sqrt(value)
